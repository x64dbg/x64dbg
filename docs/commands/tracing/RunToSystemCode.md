# RunToSystemCode/rts/RunSystem

Run until system code is reached. Equivalent to `RunToParty 1`.

Unlike `StepSystem`, this uses temporary execute-memory breakpoints rather than single-stepping. See [RunToParty](RunToParty.md) for limitations and interruption behavior.

## arguments

This command has no arguments.

## results

This command does not set any result variables.
